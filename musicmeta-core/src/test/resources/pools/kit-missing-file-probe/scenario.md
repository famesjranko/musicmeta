# kit-missing-file-probe

Not a scenario. A manifest naming a body file that is deliberately absent, so the kit can assert
that a missing pool resource fails with its path instead of yielding a silently empty fake.

No provenance: it reproduces nothing and contains no upstream data.
