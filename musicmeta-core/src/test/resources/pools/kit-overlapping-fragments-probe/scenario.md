# kit-overlapping-fragments-probe

Not a scenario. Two fragments where one contains the other, so the kit can assert that an ambiguous
manifest is rejected rather than resolved by whatever order the JSON parser hands back.

No provenance: the bodies are empty objects and reproduce nothing.
