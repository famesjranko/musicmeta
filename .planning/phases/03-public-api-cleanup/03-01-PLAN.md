---
phase: 03-public-api-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "EnrichmentType enum entries carry their default TTL directly (no external lookup)"
    - "DefaultEnrichmentEngine.ttlFor() companion function no longer exists"
    - "EnrichmentConfig.ttlOverrides overrides per-type TTL when present"
    - "EnrichmentIdentifiers.withExtra(key, value) stores arbitrary identifiers"
    - "EnrichmentIdentifiers.get(key) retrieves stored extra identifiers"
    - "SimilarArtist and PopularTrack carry EnrichmentIdentifiers instead of bare musicBrainzId"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt"
      provides: "TTL values on enum entries"
      contains: "defaultTtlMs"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt"
      provides: "ttlOverrides map"
      contains: "ttlOverrides"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt"
      provides: "extra map and accessors on EnrichmentIdentifiers"
      contains: "withExtra"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt"
      provides: "SimilarArtist and PopularTrack with EnrichmentIdentifiers"
      contains: "identifiers: EnrichmentIdentifiers"
  key_links:
    - from: "DefaultEnrichmentEngine.kt"
      to: "EnrichmentType.defaultTtlMs"
      via: "cache.put TTL argument"
      pattern: "config\\.ttlOverrides.*type\\.defaultTtlMs"
    - from: "LastFmMapper.kt"
      to: "SimilarArtist"
      via: "mapper constructs SimilarArtist with EnrichmentIdentifiers"
      pattern: "EnrichmentIdentifiers\\(musicBrainzId"
---

<objective>
Clean up public types: move TTL into EnrichmentType enum, add extensible identifier map to EnrichmentIdentifiers, and migrate SimilarArtist/PopularTrack to use EnrichmentIdentifiers instead of bare musicBrainzId.

Purpose: Consumers interact with a stable, extensible public API surface where adding new identifiers or enrichment types never requires data class changes.
Output: Modified EnrichmentType (with TTL), EnrichmentIdentifiers (with extra map), SimilarArtist/PopularTrack (with identifiers), updated engine and mappers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt

<interfaces>
<!-- Key types and contracts the executor needs. -->

From EnrichmentType.kt (current — no constructor params):
```kotlin
enum class EnrichmentType {
    ALBUM_ART, ARTIST_PHOTO, ARTIST_BACKGROUND, ARTIST_LOGO, CD_ART,
    GENRE, LABEL, RELEASE_DATE, RELEASE_TYPE, COUNTRY, SIMILAR_ARTISTS,
    ARTIST_BIO, LYRICS_SYNCED, LYRICS_PLAIN,
    TRACK_POPULARITY, ARTIST_POPULARITY,
}
```

From DefaultEnrichmentEngine.kt companion (to be removed):
```kotlin
fun ttlFor(type: EnrichmentType): Long = when (type) {
    EnrichmentType.ALBUM_ART, ... -> 90L * 24 * 60 * 60 * 1000
    EnrichmentType.ARTIST_PHOTO, ... -> 30L * 24 * 60 * 60 * 1000
    EnrichmentType.LABEL, ... -> 365L * 24 * 60 * 60 * 1000
    EnrichmentType.TRACK_POPULARITY, EnrichmentType.ARTIST_POPULARITY -> 7L * 24 * 60 * 60 * 1000
}
```

From EnrichmentIdentifiers (current — fixed fields only):
```kotlin
data class EnrichmentIdentifiers(
    val musicBrainzId: String? = null,
    val musicBrainzReleaseGroupId: String? = null,
    val wikidataId: String? = null,
    val isrc: String? = null,
    val barcode: String? = null,
    val wikipediaTitle: String? = null,
)
```

From EnrichmentData.kt (current — bare musicBrainzId):
```kotlin
data class SimilarArtist(
    val name: String,
    val musicBrainzId: String? = null,
    val matchScore: Float,
)
data class PopularTrack(
    val title: String,
    val musicBrainzId: String? = null,
    val listenCount: Long,
    val rank: Int,
)
```

From EnrichmentCache.kt (consumes TTL):
```kotlin
suspend fun put(entityKey: String, type: EnrichmentType, result: EnrichmentResult.Success, ttlMs: Long = DEFAULT_TTL_MS)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move TTL into EnrichmentType enum, add ttlOverrides to config, remove ttlFor()</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
  </read_first>
  <behavior>
    - Test: EnrichmentType.ALBUM_ART.defaultTtlMs equals 90 days in ms (7_776_000_000)
    - Test: EnrichmentType.TRACK_POPULARITY.defaultTtlMs equals 7 days in ms (604_800_000)
    - Test: EnrichmentType.LABEL.defaultTtlMs equals 365 days in ms (31_536_000_000)
    - Test: EnrichmentType.ARTIST_PHOTO.defaultTtlMs equals 30 days in ms (2_592_000_000)
    - Test: Engine uses config.ttlOverrides[type] when present, falls back to type.defaultTtlMs
    - Test: ttlFor() companion function does not exist (compile check via usage removal)
  </behavior>
  <action>
    1. Add constructor param `defaultTtlMs: Long` to `EnrichmentType` enum. Assign TTL values from the current `ttlFor()` when-block to each entry:
       - ALBUM_ART, ARTIST_LOGO, CD_ART, GENRE, LYRICS_SYNCED, LYRICS_PLAIN: `90L * 24 * 60 * 60 * 1000` (90 days)
       - ARTIST_PHOTO, ARTIST_BACKGROUND, SIMILAR_ARTISTS, ARTIST_BIO: `30L * 24 * 60 * 60 * 1000` (30 days)
       - LABEL, RELEASE_DATE, RELEASE_TYPE, COUNTRY: `365L * 24 * 60 * 60 * 1000` (365 days)
       - TRACK_POPULARITY, ARTIST_POPULARITY: `7L * 24 * 60 * 60 * 1000` (7 days)

    2. Add `ttlOverrides: Map<EnrichmentType, Long> = emptyMap()` field to `EnrichmentConfig` data class. Add KDoc: "Per-type TTL overrides in milliseconds. Overrides EnrichmentType.defaultTtlMs when present."

    3. In `DefaultEnrichmentEngine`, change the cache.put call in the `enrich()` method from `ttlFor(type)` to `config.ttlOverrides[type] ?: type.defaultTtlMs`.

    4. Remove the entire `ttlFor()` function from `DefaultEnrichmentEngine.companion`.

    5. Write tests in `DefaultEnrichmentEngineTest.kt`:
       - Test that each TTL category has correct ms value on the enum
       - Test that engine uses ttlOverrides when configured (use FakeEnrichmentCache to capture the TTL passed to put())
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.engine.DefaultEnrichmentEngineTest" -x :musicmeta-android:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "defaultTtlMs" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
    - grep -q "ttlOverrides" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
    - ! grep -q "fun ttlFor" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt
    - grep -q "type.defaultTtlMs" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt
  </acceptance_criteria>
  <done>EnrichmentType entries carry defaultTtlMs, EnrichmentConfig has ttlOverrides, DefaultEnrichmentEngine.ttlFor() is removed, cache uses config override or type default</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add extensible extra map to EnrichmentIdentifiers, migrate SimilarArtist and PopularTrack</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngine.kt
  </read_first>
  <behavior>
    - Test: EnrichmentIdentifiers().withExtra("deezerId", "123").get("deezerId") returns "123"
    - Test: EnrichmentIdentifiers().get("nonexistent") returns null
    - Test: withExtra preserves existing extra entries (immutable copy)
    - Test: withExtra preserves existing typed fields (musicBrainzId, etc.)
    - Test: SimilarArtist has identifiers: EnrichmentIdentifiers field (not musicBrainzId)
    - Test: PopularTrack has identifiers: EnrichmentIdentifiers field (not musicBrainzId)
  </behavior>
  <action>
    1. In `EnrichmentRequest.kt`, add to `EnrichmentIdentifiers`:
       - New field: `val extra: Map<String, String> = emptyMap()`
       - New method: `fun get(key: String): String? = extra[key]`
       - New method: `fun withExtra(key: String, value: String): EnrichmentIdentifiers = copy(extra = extra + (key to value))`

    2. In `DefaultEnrichmentEngine.resolveIdentity()`, update the `mergedIds` construction (around line 134) to also merge extra maps: add `extra = resolved.extra + request.identifiers.extra` (resolved takes precedence by being first in the +, but since resolved is from identity resolution which won't have extras, order doesn't matter much -- use `request.identifiers.extra + resolved.extra` to let resolved override).

    3. In `EnrichmentData.kt`, change `SimilarArtist`:
       - Replace `val musicBrainzId: String? = null` with `val identifiers: EnrichmentIdentifiers = EnrichmentIdentifiers()`
       - Keep `name` and `matchScore` fields unchanged

    4. In `EnrichmentData.kt`, change `PopularTrack`:
       - Replace `val musicBrainzId: String? = null` with `val identifiers: EnrichmentIdentifiers = EnrichmentIdentifiers()`
       - Keep `title`, `listenCount`, `rank` fields unchanged

    5. In `LastFmMapper.toSimilarArtists()`, change `musicBrainzId = it.mbid` to `identifiers = EnrichmentIdentifiers(musicBrainzId = it.mbid)`. Import EnrichmentIdentifiers.

    6. In `ListenBrainzMapper.toPopularity()`, change `musicBrainzId = track.recordingMbid` to `identifiers = EnrichmentIdentifiers(musicBrainzId = track.recordingMbid)`. Import EnrichmentIdentifiers.

    7. In `LastFmProviderTest.kt`, update the assertion at line 47 from `similar[0].musicBrainzId` to `similar[0].identifiers.musicBrainzId`.

    8. Write tests for EnrichmentIdentifiers.withExtra() and get() -- add a new test file `musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/EnrichmentIdentifiersTest.kt`.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test -x :musicmeta-android:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "withExtra" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt
    - grep -q "fun get" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt
    - grep -q "extra: Map" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentRequest.kt
    - grep -q "identifiers: EnrichmentIdentifiers" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - ! grep -q "musicBrainzId: String" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - grep -q "EnrichmentIdentifiers(musicBrainzId" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
    - grep -q "EnrichmentIdentifiers(musicBrainzId" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt
  </acceptance_criteria>
  <done>EnrichmentIdentifiers has extra map with get()/withExtra(), SimilarArtist and PopularTrack use identifiers: EnrichmentIdentifiers, mappers construct EnrichmentIdentifiers properly, all tests pass</done>
</task>

</tasks>

<verification>
1. Full test suite passes: `./gradlew :musicmeta-core:test -x :musicmeta-android:test`
2. No references to `ttlFor(` remain in DefaultEnrichmentEngine
3. EnrichmentType entries all have defaultTtlMs values
4. SimilarArtist and PopularTrack have no bare `musicBrainzId: String?` field
5. EnrichmentIdentifiers supports `withExtra` and `get`
</verification>

<success_criteria>
- EnrichmentType.ALBUM_ART.defaultTtlMs compiles and returns 90 days in ms
- config.ttlOverrides overrides per-type TTL in the engine
- DefaultEnrichmentEngine has no ttlFor() function
- EnrichmentIdentifiers("deezerId", "123").get("deezerId") == "123" via withExtra
- SimilarArtist.identifiers and PopularTrack.identifiers are EnrichmentIdentifiers
- All existing tests pass unchanged (except the one assertion path update in LastFmProviderTest)
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-api-cleanup/03-01-SUMMARY.md`
</output>
