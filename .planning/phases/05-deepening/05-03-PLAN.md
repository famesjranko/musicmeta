---
phase: 05-deepening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProviderTest.kt
autonomous: true
requirements: [DEEP-02, DEEP-04]

must_haves:
  truths:
    - "Deezer ALBUM_METADATA returns trackCount, label, genres, and explicit flag from album search data"
    - "iTunes ALBUM_METADATA returns trackCount and primaryGenreName from search results"
    - "Discogs ALBUM_METADATA returns community rating and catalog number from release search"
    - "WikipediaProvider ARTIST_PHOTO returns supplemental images from page media-list endpoint"
    - "Metadata.trackCount field is populated by Deezer, iTunes, or Discogs"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt"
      provides: "Metadata with trackCount: Int?, explicit: Boolean?, catalogNumber: String?, communityRating: Float?"
      contains: "trackCount"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt"
      provides: "ALBUM_METADATA capability"
      contains: "ALBUM_METADATA"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaApi.kt"
      provides: "getPageMediaList() method"
      contains: "getPageMediaList"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt"
      provides: "ARTIST_PHOTO capability"
      contains: "ARTIST_PHOTO"
  key_links:
    - from: "DeezerProvider.kt"
      to: "DeezerMapper.toAlbumMetadata()"
      via: "enrichAlbumMetadata -> toAlbumMetadata"
      pattern: "toAlbumMetadata"
    - from: "WikipediaProvider.kt"
      to: "WikipediaApi.getPageMediaList()"
      via: "enrichArtistPhoto -> getPageMediaList"
      pattern: "getPageMediaList"
---

<objective>
Mine more album metadata from Deezer, iTunes, and Discogs responses, and add Wikipedia page media as supplemental ARTIST_PHOTO source.

Purpose: Existing API responses from Deezer, iTunes, and Discogs contain album metadata fields (trackCount, label, genres, explicit flag, community rating, catalog number) that are currently ignored. Wikipedia articles contain images that supplement thin Wikidata ARTIST_PHOTO coverage.

Output: Three providers gain ALBUM_METADATA capability extracting previously ignored fields. WikipediaProvider gains ARTIST_PHOTO capability via page media-list endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaModels.kt

<interfaces>
From EnrichmentData.kt - current Metadata:
```kotlin
data class Metadata(
    val genres: List<String>? = null,
    val label: String? = null,
    val releaseDate: String? = null,
    val releaseType: String? = null,
    val country: String? = null,
    val barcode: String? = null,
    val disambiguation: String? = null,
    val artistType: String? = null,
    val beginDate: String? = null,
    val endDate: String? = null,
    val isrc: String? = null,
) : EnrichmentData()
```

From EnrichmentType.kt - ALBUM_METADATA does NOT exist yet. Will need to check if it exists or if existing types (GENRE, LABEL, etc.) are used for album metadata. Looking at the codebase, album metadata is currently returned per-field via GENRE, LABEL, RELEASE_DATE, etc. But the PRD section 4.2 says "Add these to Metadata where appropriate" -- this means enriching the existing metadata types, not creating a new ALBUM_METADATA type.

HOWEVER, looking at the roadmap success criteria: "enrich(ForAlbum(...), setOf(ALBUM_METADATA)) populates trackCount and label" -- this implies a new ALBUM_METADATA type IS expected. Let me add it.

From DeezerAlbumResult (what Deezer already parses from search):
```kotlin
data class DeezerAlbumResult(
    val id: Long = 0, val title: String, val artistName: String,
    val coverSmall: String?, val coverMedium: String?, val coverBig: String?, val coverXl: String?,
)
```
Note: Deezer search returns more fields (nb_tracks, record_type, genre_id, explicit_lyrics, label) but they are currently NOT parsed.

From ITunesAlbumResult (what iTunes already parses):
```kotlin
data class ITunesAlbumResult(
    val collectionId: Long = 0, val collectionName: String, val artistName: String,
    val artworkUrl: String?, val releaseDate: String? = null,
    val primaryGenreName: String? = null, val country: String? = null,
)
```
Note: iTunes also returns trackCount, collectionPrice but they are NOT parsed.

From DiscogsRelease (what Discogs already parses):
```kotlin
data class DiscogsRelease(
    val title: String, val label: String?, val year: String?,
    val country: String?, val coverImage: String?, val releaseType: String? = null,
)
```
Note: Discogs search also returns community rating, catno, genre, style but they are NOT parsed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Album metadata from Deezer, iTunes, and Discogs with new Metadata fields</name>
  <read_first>
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProviderTest.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProviderTest.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/EnrichmentDataSerializationTest.kt
  </read_first>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  </files>
  <behavior>
    - Test: Deezer ALBUM_METADATA returns Metadata with trackCount, label, genres, explicit flag
    - Test: iTunes ALBUM_METADATA returns Metadata with trackCount, primaryGenreName
    - Test: Discogs ALBUM_METADATA returns Metadata with community rating, catalog number, genres, styles
    - Test: Metadata serialization round-trip with new fields
  </behavior>
  <action>
    1. In EnrichmentType.kt, add `ALBUM_METADATA(90L * 24 * 60 * 60 * 1000),` in the "Structured metadata" section after COUNTRY.

    2. In EnrichmentData.kt, add new fields to Metadata:
       ```kotlin
       data class Metadata(
           val genres: List<String>? = null,
           val label: String? = null,
           val releaseDate: String? = null,
           val releaseType: String? = null,
           val country: String? = null,
           val barcode: String? = null,
           val disambiguation: String? = null,
           val artistType: String? = null,
           val beginDate: String? = null,
           val endDate: String? = null,
           val isrc: String? = null,
           val trackCount: Int? = null,
           val explicit: Boolean? = null,
           val catalogNumber: String? = null,
           val communityRating: Float? = null,
       ) : EnrichmentData()
       ```

    3. **Deezer:** Parse extra fields from album search response.
       - In DeezerModels.kt, add to `DeezerAlbumResult`:
         ```kotlin
         val nbTracks: Int? = null,
         val recordType: String? = null,
         val label: String? = null,
         val explicitLyrics: Boolean? = null,
         val genreId: Int? = null,
         ```
       - In DeezerApi.kt `searchAlbums()`, parse these extra fields from the JSON:
         `album.optInt("nb_tracks", 0).takeIf { it > 0 }`, `album.optString("record_type").takeIfNotEmpty()`, `album.optBoolean("explicit_lyrics", false)`. Note: label is not in search results but is in the album detail. For now, parse what's available.
       - In DeezerMapper.kt, add `toAlbumMetadata()`:
         ```kotlin
         fun toAlbumMetadata(result: DeezerAlbumResult): EnrichmentData.Metadata =
             EnrichmentData.Metadata(
                 trackCount = result.nbTracks,
                 releaseType = result.recordType,
                 explicit = result.explicitLyrics,
             )
         ```
       - In DeezerProvider.kt:
         - Add capability: `ProviderCapability(EnrichmentType.ALBUM_METADATA, priority = 50)`
         - Add case in `enrich()` for ALBUM_METADATA that searches for the album and returns metadata:
           ```kotlin
           EnrichmentType.ALBUM_METADATA -> enrichAlbumMetadata(request, type)
           ```
         - Add `enrichAlbumMetadata()` method using the same search approach as `enrichAlbumArt`.

    4. **iTunes:** Parse trackCount from search results.
       - In ITunesModels.kt, add to ITunesAlbumResult:
         ```kotlin
         val trackCount: Int? = null,
         val collectionPrice: Double? = null,
         ```
       - In ITunesApi.kt `searchAlbums()`, parse: `album.optInt("trackCount", 0).takeIf { it > 0 }`, `album.optDouble("collectionPrice").takeIf { !it.isNaN() }`
       - In ITunesMapper.kt, add `toAlbumMetadata()`:
         ```kotlin
         fun toAlbumMetadata(result: ITunesAlbumResult): EnrichmentData.Metadata =
             EnrichmentData.Metadata(
                 trackCount = result.trackCount,
                 genres = listOfNotNull(result.primaryGenreName),
                 country = result.country,
                 releaseDate = result.releaseDate,
             )
         ```
       - In ITunesProvider.kt:
         - Add capability: `ProviderCapability(EnrichmentType.ALBUM_METADATA, priority = 30)`
         - Add case in `enrich()` for ALBUM_METADATA using same search + artist matching pattern as album art.

    5. **Discogs:** Parse community rating, catno, genres, styles from search results.
       - In DiscogsModels.kt, add to DiscogsRelease:
         ```kotlin
         val catno: String? = null,
         val genres: List<String>? = null,
         val styles: List<String>? = null,
         ```
       - In DiscogsApi.kt `parseReleaseResults()`, parse:
         `obj.optString("catno").takeIf { it.isNotBlank() }`,
         parse `genre` array: `obj.optJSONArray("genre")?.let { arr -> (0 until arr.length()).map { arr.getString(it) } }`,
         parse `style` array similarly.
       - In DiscogsMapper.kt, add `toAlbumMetadata()`:
         ```kotlin
         fun toAlbumMetadata(release: DiscogsRelease): EnrichmentData.Metadata =
             EnrichmentData.Metadata(
                 label = release.label,
                 releaseDate = release.year,
                 releaseType = release.releaseType,
                 country = release.country,
                 catalogNumber = release.catno,
                 genres = (release.genres.orEmpty() + release.styles.orEmpty()).takeIf { it.isNotEmpty() },
             )
         ```
       - In DiscogsProvider.kt:
         - Add capability: `ProviderCapability(EnrichmentType.ALBUM_METADATA, priority = 40)`
         - Add ALBUM_METADATA to the `enrichFromRelease()` when block.

    6. Add tests in each provider test file for ALBUM_METADATA enrichment with canned JSON responses that include the new fields. Ensure the tests verify that Metadata contains trackCount, genres, etc.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.deezer.DeezerProviderTest" --tests "com.landofoz.musicmeta.provider.itunes.ITunesProviderTest" --tests "com.landofoz.musicmeta.provider.discogs.DiscogsProviderTest" -x :musicmeta-android:test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - EnrichmentType.ALBUM_METADATA exists
    - Metadata has trackCount, explicit, catalogNumber, communityRating fields
    - DeezerProvider.capabilities includes ALBUM_METADATA
    - ITunesProvider.capabilities includes ALBUM_METADATA
    - DiscogsProvider.capabilities includes ALBUM_METADATA
    - Each provider's ALBUM_METADATA returns Metadata with their specific fields populated
  </acceptance_criteria>
  <done>Three providers (Deezer, iTunes, Discogs) serve ALBUM_METADATA with previously ignored fields. Metadata data class has new trackCount, explicit, catalogNumber, communityRating fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wikipedia page media-list for supplemental ARTIST_PHOTO</name>
  <read_first>
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProviderTest.kt
  </read_first>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProviderTest.kt
  </files>
  <behavior>
    - Test: getPageMediaList("Radiohead") returns list of WikipediaMediaItem with image URLs
    - Test: WikipediaMapper.toArtwork maps best media item to Artwork
    - Test: enrich(ForArtist, ARTIST_PHOTO) returns Artwork when media-list has images
    - Test: enrich(ForArtist, ARTIST_PHOTO) returns NotFound when no suitable images
    - Test: SVG and icon images are filtered out (only JPEG/PNG)
  </behavior>
  <action>
    1. In WikipediaModels.kt, add:
       ```kotlin
       data class WikipediaMediaItem(
           val title: String,
           val url: String,
           val width: Int?,
           val height: Int?,
       )
       ```

    2. In WikipediaApi.kt, add `getPageMediaList()`:
       ```kotlin
       suspend fun getPageMediaList(title: String): List<WikipediaMediaItem> = rateLimiter.execute {
           val encoded = URLEncoder.encode(title, "UTF-8").replace("+", "%20")
           val url = "$MEDIA_LIST_BASE_URL/$encoded"
           val json = httpClient.fetchJson(url) ?: return@execute emptyList()
           parseMediaList(json)
       }

       private fun parseMediaList(json: JSONObject): List<WikipediaMediaItem> {
           val items = json.optJSONArray("items") ?: return emptyList()
           val results = mutableListOf<WikipediaMediaItem>()
           for (i in 0 until items.length()) {
               val item = items.optJSONObject(i) ?: continue
               val type = item.optString("type", "")
               if (type != "image") continue
               val title = item.optString("title", "")
               // Filter out SVGs, icons, logos, and tiny images
               if (title.endsWith(".svg", ignoreCase = true)) continue
               if (title.contains("icon", ignoreCase = true)) continue
               if (title.contains("logo", ignoreCase = true)) continue
               val srcset = item.optJSONObject("srcset")
               val originalSrc = item.optJSONObject("original")
               val imageUrl = originalSrc?.optString("source")
                   ?: srcset?.optString("source")
                   ?: continue
               if (imageUrl.isBlank()) continue
               val width = originalSrc?.optInt("width")?.takeIf { it > 0 }
               val height = originalSrc?.optInt("height")?.takeIf { it > 0 }
               // Skip tiny images (likely icons)
               if (width != null && width < 100) continue
               results.add(WikipediaMediaItem(title = title, url = imageUrl, width = width, height = height))
           }
           return results
       }
       ```
       Add constant: `const val MEDIA_LIST_BASE_URL = "https://en.wikipedia.org/api/rest_v1/page/media-list"` in the companion object.

       NOTE: The actual Wikipedia media-list API response format may differ. The executor should check the real API format. The endpoint is `GET /page/media-list/{title}` and returns `{"items": [...]}` where each item has `type`, `title`, and image source info. Adjust parsing to match the real response schema.

    3. In WikipediaMapper.kt, add:
       ```kotlin
       fun toArtwork(media: WikipediaMediaItem): EnrichmentData.Artwork =
           EnrichmentData.Artwork(
               url = media.url,
               width = media.width,
               height = media.height,
           )
       ```

    4. In WikipediaProvider.kt:
       - Add ARTIST_PHOTO capability with low priority (supplemental, behind Wikidata and Fanart.tv):
         ```kotlin
         ProviderCapability(
             type = EnrichmentType.ARTIST_PHOTO,
             priority = 30,
             identifierRequirement = IdentifierRequirement.WIKIPEDIA_TITLE,
         ),
         ```
       - Update `enrich()` to handle ARTIST_PHOTO:
         ```kotlin
         override suspend fun enrich(request: EnrichmentRequest, type: EnrichmentType): EnrichmentResult {
             val title = request.identifiers.wikipediaTitle
                 ?: resolveFromWikidata(request.identifiers.wikidataId)
             if (title == null) return EnrichmentResult.NotFound(type, id)

             return when (type) {
                 EnrichmentType.ARTIST_BIO -> enrichBio(title, type)
                 EnrichmentType.ARTIST_PHOTO -> enrichArtistPhoto(title, type)
                 else -> EnrichmentResult.NotFound(type, id)
             }
         }
         ```
       - Extract existing bio logic into `enrichBio()`:
         ```kotlin
         private suspend fun enrichBio(title: String, type: EnrichmentType): EnrichmentResult {
             val summary = api.getPageSummary(title) ?: return EnrichmentResult.NotFound(type, id)
             return EnrichmentResult.Success(type = type, data = WikipediaMapper.toBiography(summary), provider = id, confidence = CONFIDENCE)
         }
         ```
       - Add `enrichArtistPhoto()`:
         ```kotlin
         private suspend fun enrichArtistPhoto(title: String, type: EnrichmentType): EnrichmentResult {
             val mediaItems = api.getPageMediaList(title)
             val bestImage = mediaItems.firstOrNull() ?: return EnrichmentResult.NotFound(type, id)
             return EnrichmentResult.Success(
                 type = type,
                 data = WikipediaMapper.toArtwork(bestImage),
                 provider = id,
                 confidence = PHOTO_CONFIDENCE,
             )
         }
         ```
       - Add constant: `const val PHOTO_CONFIDENCE = 0.7f` (lower than Wikidata 0.9 since Wikipedia images are less curated).

    5. In WikipediaProviderTest.kt, add tests:
       - Test ARTIST_PHOTO with canned media-list JSON containing image items
       - Test ARTIST_PHOTO returns NotFound when media list has no images
       - Test SVG images are filtered out
       - Test existing ARTIST_BIO tests still pass
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.wikipedia.WikipediaProviderTest" -x :musicmeta-android:test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - WikipediaApi has getPageMediaList() calling /page/media-list/{title}
    - WikipediaModels.kt has WikipediaMediaItem data class
    - WikipediaProvider.capabilities includes ARTIST_PHOTO at priority 30
    - enrich(ForArtist, ARTIST_PHOTO) returns Artwork from page media when images exist
    - SVG and icon images are filtered out
    - Existing ARTIST_BIO capability still works
  </acceptance_criteria>
  <done>WikipediaProvider serves ARTIST_PHOTO via page media-list endpoint as supplemental source (priority 30, behind Wikidata 100 and Fanart.tv 80). SVG/icon images filtered.</done>
</task>

</tasks>

<verification>
```bash
cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test -x :musicmeta-android:test 2>&1 | tail -10
```
All tests pass. Album metadata enriched from three providers. Wikipedia serves supplemental artist photos.
</verification>

<success_criteria>
- ALBUM_METADATA type available from Deezer (priority 50), Discogs (priority 40), iTunes (priority 30)
- Metadata has trackCount, explicit, catalogNumber, communityRating fields
- WikipediaProvider has ARTIST_PHOTO capability via page media-list
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-deepening/05-03-SUMMARY.md`
</output>
