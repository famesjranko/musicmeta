*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/*/build
/captures
.externalNativeBuild
.cxx
local.properties
# Tracked: .claude/settings.json (the format-on-write hook) and .claude/rules/. A hook that is not
# committed does not reach worktrees or a fresh clone, which is most of where the agent works.
.claude/*
!.claude/settings.json
!.claude/rules/
.planning
secrets.properties

# Python bytecode (scripts/)
__pycache__/
*.pyc
