version: 2
updates:
  # Keeps the SHA-pinned actions in the release workflows current, which is what makes pinning
  # maintainable. dev, not the default branch: main is release-only and merge-commit gated.
  - package-ecosystem: github-actions
    directory: /
    target-branch: dev
    schedule:
      interval: weekly
